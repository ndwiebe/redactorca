{
  "name": "redactor-ca",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Local-first, Canadian-tax-aware PII redactor for CPAs. 100% in-browser — no upload, no backend.",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test": "vitest run"
  },
  "devDependencies": {
    "@fontsource/hanken-grotesk": "^5.2.8",
    "@fontsource/jetbrains-mono": "^5.2.8",
    "typescript": "^5.6.0",
    "vite": "^6.0.0",
    "vitest": "^2.1.0"
  },
  "dependencies": {
    "pdfjs-dist": "^6.0.227"
  }
}
